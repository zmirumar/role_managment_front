import styled from "styled-components";
import { Layout } from "antd";

export const LayoutStyled = styled(Layout)`
  min-height: 100vh;

  .inner-content {
    padding: 24px;
    background: #f0f2f5;
    min-height: 100vh;
  }
`;
